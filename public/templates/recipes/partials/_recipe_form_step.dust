
  <div class="controls">
    <label for="steps">{offset}.  </label>
    <input name="steps" value="{step}" tabindex="100{offset}">
    <span id="step-error" class="help-inline">{@error id="steps.{index}"}{.}{/error}</span>
    <a href="#" class="delete_step"><i class="icon-trash"></i></a>
  </div>

